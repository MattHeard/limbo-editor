#!/usr/bin/env ruby
subcommand = ARGV[0]
new_description = 'creates a new project'
general_description = 'Limbo EDitor'
if subcommand == 'new'
  puts new_description
else
  puts general_description
end
